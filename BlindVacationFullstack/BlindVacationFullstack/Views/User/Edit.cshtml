@model BlindVacationFullstack.Models.User
<html>
<head>
    <title></title>
</head>
<body>
    <header></header>
    <form asp-action="Edit">
        <fieldset>
            <input  type="hidden" asp-for="@Model.ID" />
        </fieldset>
        <fieldset>
            <label asp-for="@Model.Name"></label>
            <input asp-for="@Model.Name"/>
        </fieldset>

        <fieldset>
            <label asp-for="@Model.FaveTripItem">TripItem</label>
            <select asp-for="@Model.FaveTripItem" asp-items="@Html.GetEnumSelectList(typeof(BlindVacationFullstack.Models.User.TripItem))"> </select>
        </fieldset>
        <button type="submit" value="Edit">Submit Changes</button>

    </form>
    <footer></footer>
</body>
</html>